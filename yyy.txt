#include<stdio.h>
#include<stdlib.h>

int main()
{
int n;
printf("Enter the number of elements:\n");
scanf("%d",&n);
return 0;
}
int fib(int n)
{
if(n<0)
return 0;
else if (n<1)
{
return 1;
}
else
{
return (fib(n-1) + fib(n-2));
}
}